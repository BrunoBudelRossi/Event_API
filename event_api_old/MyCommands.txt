mix phx.new name (name is lowcase)

mix ecto.create     // cria base de dados (senha alterada para docker pois esta rodando la)
A base de dados criada tem o nome: EventApi.repo  por padrão, no postbird ta **event_api_dev**, lembrar que esta base com nome modificado foi criada na mão no postbird

mix phx.server     // roda o server localhost:4000

mix phx.gen.json Management Event events title:string description:text day:date

mix ecto.migrate // é sugerido mas não executei

** anotações uteis **

no video ele vai criar 2 models, products e documents onde um project tem várioas documents (UM BLOG)
ele usa os campos title:string e description: text
eu usei os campos title:string, description: text e day:date

Onde é project usa event, onde é document usar article

